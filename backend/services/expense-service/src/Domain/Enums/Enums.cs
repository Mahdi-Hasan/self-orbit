namespace SelfOrbit.ExpenseService.Domain.Enums;

public enum ExpenseStatus
{
    Draft = 0,
    Confirmed = 1,
    Archived = 2
}

public enum BudgetPeriod
{
    Daily = 0,
    Weekly = 1,
    Monthly = 2,
    Yearly = 3
}
